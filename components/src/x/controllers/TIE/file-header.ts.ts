import { FileHeader } from "../../model/TIE";

export class TIEFileHeaderController {
  public static fields: any[] = [];

  constructor(public model: FileHeader){}

  public render(field: string){}
}